#include "headers/crbn_graphEstimation.hpp"
#include <iostream>
int main()
{
    crbn::est::tagListGenerator();

    

    // int x[] = {1, 2, 3, 4, 5};
    // int y[] = {10, 20, 30, 40, 50};

    // std::cout << " y = mx + c : \n\t y = "
    //           << linearRegression({x, y}, 5).first
    //           << "x + "
    //           << linearRegression({x, y}, 5).second
    //           << "\n";

    // linearRegression({x, y}, 5);

    return 0;
}